#include "main.h"
#include <stdio.h>
/**
 * mul:-multiplies two integers
 * @a: the first number
 * @b: the second number
 * Return:result of multiplication
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
